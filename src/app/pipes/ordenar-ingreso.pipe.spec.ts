import { OrdenarIngresoPipe } from './ordenar-ingreso.pipe';

describe('OrdenarIngresoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarIngresoPipe();
    expect(pipe).toBeTruthy();
  });
});
